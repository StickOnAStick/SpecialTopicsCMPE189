HW1 is just installing and running the examples seen on the ns3 documentation. 

See the submodule @fa83b3 for the built code.


## Running

Basic command line input is as follows: 

```bash
./ns3 run '<ns3-program> --arg1=value1 --arg2=value2 ...'
# OR
./ns3 run <ns3-program> --command-template="%s <args>"
```